<UserControl x:Class="Привязка_и_команды.Views.TwoWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,16,0,0"
                    Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
            <Border Style="{StaticResource SectionStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Margin="0,0,12,0">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Двухсторонняя привязка через ViewModel" />
                        <TextBlock Style="{StaticResource CaptionTextStyle}" Text="Поля изменяют источник, а источник обновляет элементы." />
                        <TextBlock Text="Имя пользователя" />
                        <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Text="Рейтинг" />
                        <Slider Minimum="0" Maximum="100" TickFrequency="10" IsSnapToTickEnabled="True"
                                Value="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <CheckBox Content="Включить уведомления"
                                  IsChecked="{Binding NotificationsEnabled, Mode=TwoWay}"
                                  Margin="0,8,0,8" />
                        <ComboBox ItemsSource="{Binding ThemeOptions}"
                                  SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"
                                  Width="220"
                                  HorizontalAlignment="Left" />
                        <Button Content="Сохранить через команду" Command="{Binding SaveTwoWayCommand}" />
                        <TextBlock Text="{Binding SaveMessage}" FontWeight="SemiBold" />
                    </StackPanel>

                    <Border Grid.Column="1" Background="{StaticResource PanelBrush}" CornerRadius="8" Padding="14">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Состояние модели" />
                            <TextBlock Text="{Binding UserName, StringFormat=Имя: {0}}" />
                            <TextBlock Text="{Binding Rating, StringFormat=Рейтинг: {0}}" />
                            <TextBlock Text="{Binding NotificationsEnabled, StringFormat=Уведомления: {0}}" />
                            <TextBlock Text="{Binding SelectedTheme, StringFormat=Тема: {0}}" />
                            <ProgressBar Minimum="0" Maximum="100" Height="14" Margin="0,8,0,0"
                                         Value="{Binding Rating}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource SectionStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,12,0">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Прямая двухсторонняя привязка" />
                        <TextBox x:Name="DirectBox" Text="Текст элемента" />
                        <TextBox Text="{Binding ElementName=DirectBox, Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Через визуальную модель" />
                        <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Text="{Binding UserName, StringFormat=Источник ViewModel: {0}}" />
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
